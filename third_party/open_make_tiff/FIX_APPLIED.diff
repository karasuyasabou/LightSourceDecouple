diff --git a/pkg/manager/manager.go b/pkg/manager/manager.go
index a6a154b..59cd739 100644
--- a/pkg/manager/manager.go
+++ b/pkg/manager/manager.go
@@ -77,6 +77,7 @@ type Manager struct {
 	et      *exiftool.Exiftool
 	wg      sync.WaitGroup
 	emit    EventEmitter
+	gui     bool
 
 	tmpDir                 *util.TempDir
 	dngConverterExecutable string
@@ -152,10 +153,11 @@ func (m *Manager) OnStartup(ctx context.Context) {
 	ctx, cancel := context.WithCancel(ctx)
 	m.ctx = ctx
 	m.cancel = cancel
+	m.gui = true
 
 	m.loadConfig()
 	m.validateConfig()
-	wails_runtime.WindowSetAlwaysOnTop(m.ctx, m.config.EnableWindowTop)
+	m.setAlwaysOnTop(m.config.EnableWindowTop)
 
 	if m.emit == nil {
 		m.emit = func(event string, data ...any) {
@@ -185,7 +187,7 @@ func (m *Manager) OnSecondInstanceLaunch(_ options.SecondInstanceData) {
 
 	m.loadConfig()
 	m.validateConfig()
-	wails_runtime.WindowSetAlwaysOnTop(m.ctx, m.config.EnableWindowTop)
+	m.setAlwaysOnTop(m.config.EnableWindowTop)
 }
 
 func (m *Manager) Shutdown() {
@@ -238,10 +240,17 @@ func (m *Manager) SetConfig(cfg *Config) *Config {
 
 	m.validateConfig()
 	m.saveConfig()
-	wails_runtime.WindowSetAlwaysOnTop(m.ctx, cfg.EnableWindowTop)
+	m.setAlwaysOnTop(cfg.EnableWindowTop)
 	return m.config
 }
 
+func (m *Manager) setAlwaysOnTop(enable bool) {
+	if !m.gui {
+		return
+	}
+	wails_runtime.WindowSetAlwaysOnTop(m.ctx, enable)
+}
+
 func (m *Manager) configPath() string {
 	path, err := os.UserConfigDir()
 	if err != nil {
